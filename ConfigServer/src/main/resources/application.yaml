server:
  port: 8088

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/manesuraj/neosoft-micro-configuration
          clone-on-start: true

